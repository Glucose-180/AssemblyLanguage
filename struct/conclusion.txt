	对齐是按照2^m对齐的（二进制数后面全是0），struct本身也不是按自己的长度对齐。我没理解错的话，应该是struct按照其中元素的最大对齐长度，设置自己的对齐长度。——吉骏雄
	在 struct.c 中，s1 是 8 字节对齐的（64 位），所以 s2 的对齐规则取 s 与 c 较大者，即 s，所以 s2 也是 8 字节对齐的。s1 与 s2 的大小分别为 16、40。s2 的大小为 40 表明，s[1] 未使用的 9~15 字节不能留给 c 使用。
